[[plugins]]
name = "Mars" # This is not required if the folder is the same as the plugin name.
folder = "do-names-even-matter"
description = "Planet plugin. Tells you which planet you are probably on."
min_bot_version = "v0.1.0"

[[plugins]]
name = "Internal Eval"
folder = 'internal_eval'
